# Task Manager API

Простое и эффективное REST API для управления задачами, построенное на современном стеке .NET.

## 🚀 Технологии

- **Платформа**: .NET 8
- **Фреймворк**: ASP.NET Core (Minimal API)
- **База данных**: SQLite с Entity Framework Core 8
- **Аутентификация**: Планируется JWT (в разработке)

## 🔧 Функциональность

### Основные возможности
- **Полный CRUD** для задач (создание, чтение, обновление, удаление)
- **Пагинация** с метаданными (общее количество, страницы)
- **Фильтрация** по статусу и текстовому поиску
- **Валидация данных** через DataAnnotations
- **Сидирование** тестовых данных при первом запуске

### Эндпоинты API
**GET /tasks** # Список задач с пагинацией
**GET /tasks/{id}** # Получение конкретной задачи
**POST /tasks** # Создание новой задачи
**PUT /tasks/{id}** # Обновление задачи
**DELETE /tasks/{id}** # Удаление задачи

## 🛠️ Запуск проекта

### Предварительные требования
- [.NET 8 SDK](https://dotnet.microsoft.com/download/dotnet/8.0)
- IDE (Visual Studio 2022, VS Code или Rider)

### Локальный запуск
```bash
# Клонирование репозитория
git clone https://github.com/Dred-limit/TaskManager.git
cd TaskManager/TaskManagerApi

# Восстановление зависимостей
dotnet restore

# Запуск приложения
dotnet run
